# Dependency Auditor Skill

## 概述

这个 Skill 用于审计项目依赖的安全性，包括安全漏洞扫描、过期依赖检测和许可证合规性检查。

## 文件结构

```
skillsets/dependency-auditor/
├── SKILL.md          # Skill 定义文件
├── impl.py           # 实现脚本
├── test_skill.py     # 测试脚本
└── README.md         # 使用文档
```

## 如何使用

### 方式 1: 直接运行脚本

```bash
python3 skillsets/dependency-auditor/impl.py
```

### 方式 2: 运行测试脚本

```bash
python3 skillsets/dependency-auditor/test_skill.py
```

### 方式 3: 通过 Skill 触发

在对话中使用以下短语：
- "检查依赖安全"
- "扫描依赖漏洞"
- "依赖更新建议"
- "许可证合规检查"

## 支持的包管理器

| 包管理器 | 语言 | 配置文件 | 审计命令 |
|---------|------|---------|---------|
| npm | Node.js | package.json | `npm audit` |
| pip | Python | requirements.txt | `pip-audit` |
| cargo | Rust | Cargo.toml | `cargo audit` |
| composer | PHP | composer.json | `composer audit` |
| maven | Java | pom.xml | 需额外工具 |
| gradle | Java | build.gradle | 需额外工具 |

## 输出文件

执行后会生成 `dependency_audit_report.txt` 文件，包含：
- 包管理器检测结果
- 安全漏洞详情（按严重性分组）
- 过期依赖列表
- 许可证合规性分析
- 更新建议和优先级

## 可选工具安装

### Python (pip)
```bash
pip install pip-audit
pip install pip-licenses
```

### Rust (cargo)
```bash
cargo install cargo-audit
cargo install cargo-outdated
```

### Node.js (npm)
npm audit 已内置，无需额外安装

## 分析标准

### 安全漏洞分级
- **Critical (关键)**: 远程代码执行、SQL注入、认证绕过
- **High (高危)**: 权限提升、数据泄露、XSS
- **Medium (中危)**: 拒绝服务、信息泄露
- **Low (低危)**: 次要问题、可选加固

### 许可证分类
- **宽松许可**: MIT, Apache-2.0, BSD (通常安全)
- **弱 Copyleft**: LGPL, MPL (可能需要署名)
- **强 Copyleft**: GPL, AGPL (可能需要开源)
- **风险许可**: SSPL, CPAL (商业限制)

## 测试状态

✅ 所有测试通过

## 依赖

- Python 3.x
- 可选：npm, pip-audit, cargo-audit, composer audit

## 最佳实践

1. **定期审计**: 建议每月运行一次安全审计
2. **CI/CD 集成**: 在部署前自动运行安全扫描
3. **及时更新**: 优先修复关键和高危漏洞
4. **许可证审查**: 确保所有依赖许可证与项目兼容
5. **版本锁定**: 生产环境使用锁定版本

## 示例输出

```
================================================================================
🔍 依赖安全审计工具
⏰ 开始时间: 2025-01-30 10:00:00
📁 工作目录: /path/to/project
================================================================================

================================================================================
🔍 检测包管理器
================================================================================
✅ 检测到 npm (package.json)
✅ 检测到 pip (requirements.txt)

...
```

## 下一步

1. 查看生成的 `dependency_audit_report.txt` 获取详细分析
2. 根据报告建议更新依赖或修复漏洞
3. 将审计集成到 CI/CD 流程中
4. 定期重新运行此审计以跟踪依赖状态
