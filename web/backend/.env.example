# Bingo Downloader Web - Environment Configuration Example
# Copy this file to .env and configure according to your needs

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
HOST=0.0.0.0
PORT=8000
RELOAD=true

# =============================================================================
# SECURITY - API KEY AUTHENTICATION (OPTIONAL)
# =============================================================================
# Enable/disable API key authentication (default: false)
# When enabled, all API requests require X-API-Key header
API_KEY_ENABLED=false

# Name of the API key header (default: X-API-Key)
API_KEY_NAME=X-API-Key

# Comma-separated list of valid API keys (only used when API_KEY_ENABLED=true)
# Generate secure random keys, e.g., using: openssl rand -hex 32
API_KEYS=your-secret-api-key-1,your-secret-api-key-2

# =============================================================================
# SECURITY - RATE LIMITING
# =============================================================================
# Enable/disable rate limiting (default: true)
RATE_LIMIT_ENABLED=true

# Number of requests allowed per time window (default: 60)
RATE_LIMIT_REQUESTS=60

# Time window in seconds (default: 60)
RATE_LIMIT_WINDOW=60

# =============================================================================
# SECURITY - CORS (Cross-Origin Resource Sharing)
# =============================================================================
# Comma-separated list of allowed origins
# For production, specify exact domains. Use "*" to allow all (not recommended)
# Examples:
#   CORS_ORIGINS=http://localhost:8000,https://yourdomain.com
#   CORS_ORIGINS=*  # Not recommended for production
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Allow credentials in CORS requests (default: true)
CORS_ALLOW_CREDENTIALS=true

# Allowed HTTP methods (default: GET,POST,OPTIONS)
CORS_ALLOW_METHODS=GET,POST,OPTIONS

# Allowed headers (default: Content-Type,Authorization,X-API-Key)
CORS_ALLOW_HEADERS=Content-Type,Authorization,X-API-Key

# =============================================================================
# SECURITY - COOKIE ENCRYPTION
# =============================================================================
# Optional: Provide a custom encryption key for cookie storage
# If not provided, a key will be auto-generated and stored in ~/.bingo-downloader/.encryption_key
# Generate a secure key with: openssl rand -base64 32
# COOKIE_ENCRYPTION_KEY=your-32-byte-base64-encoded-key

# Cookie expiration time in hours (default: 24)
# After this time, cookies will need to be re-authorized
COOKIE_EXPIRATION_HOURS=24

# =============================================================================
# DOWNLOAD SETTINGS
# =============================================================================
# Default video quality (360, 480, 720, 1080, best)
DEFAULT_QUALITY=1080

# Default browser for cookies (chrome, firefox, safari, edge)
DEFAULT_COOKIES_BROWSER=chrome

# Maximum file size warning in bytes (default: 2GB)
MAX_FILE_SIZE_WARNING=2147483648

# =============================================================================
# RETRY SETTINGS
# =============================================================================
# Maximum retry attempts for failed downloads
MAX_RETRY_ATTEMPTS=3

# Initial retry delay in seconds
INITIAL_RETRY_DELAY=5

# Retry backoff multiplier
RETRY_BACKOFF_MULTIPLIER=2
